package com.gindevp.app.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class RepairRequestAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertRepairRequestAllPropertiesEquals(RepairRequest expected, RepairRequest actual) {
        assertRepairRequestAutoGeneratedPropertiesEquals(expected, actual);
        assertRepairRequestAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertRepairRequestAllUpdatablePropertiesEquals(RepairRequest expected, RepairRequest actual) {
        assertRepairRequestUpdatableFieldsEquals(expected, actual);
        assertRepairRequestUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertRepairRequestAutoGeneratedPropertiesEquals(RepairRequest expected, RepairRequest actual) {
        assertThat(expected)
            .as("Verify RepairRequest auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertRepairRequestUpdatableFieldsEquals(RepairRequest expected, RepairRequest actual) {
        assertThat(expected)
            .as("Verify RepairRequest relevant properties")
            .satisfies(e -> assertThat(e.getCode()).as("check code").isEqualTo(actual.getCode()))
            .satisfies(e -> assertThat(e.getRequestDate()).as("check requestDate").isEqualTo(actual.getRequestDate()))
            .satisfies(e -> assertThat(e.getProblemCategory()).as("check problemCategory").isEqualTo(actual.getProblemCategory()))
            .satisfies(e -> assertThat(e.getDescription()).as("check description").isEqualTo(actual.getDescription()))
            .satisfies(e -> assertThat(e.getStatus()).as("check status").isEqualTo(actual.getStatus()))
            .satisfies(e -> assertThat(e.getResolutionNote()).as("check resolutionNote").isEqualTo(actual.getResolutionNote()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertRepairRequestUpdatableRelationshipsEquals(RepairRequest expected, RepairRequest actual) {
        assertThat(expected)
            .as("Verify RepairRequest relationships")
            .satisfies(e -> assertThat(e.getRequester()).as("check requester").isEqualTo(actual.getRequester()))
            .satisfies(e -> assertThat(e.getEquipment()).as("check equipment").isEqualTo(actual.getEquipment()));
    }
}
